# Copy this file to .env for local development.
# On Render, set these in the service's env vars at BUILD time:
#   VITE_API_URL = https://your-backend.onrender.com/api

# Local backend URL.
VITE_API_URL=http://localhost:5000/api

# Optional: only used to override the site name shown in the UI.
VITE_APP_NAME=Velmora
